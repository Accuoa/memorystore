{
  "name": "memorystore",
  "version": "0.1.0",
  "description": "Self-hosted MCP-native memory backend for AI assistants. SQLite + sqlite-vec, Ollama embeddings, zero external network calls.",
  "private": true,
  "type": "module",
  "scripts": {
    "start": "node src/server.mjs",
    "mcp": "node src/mcp.mjs",
    "test": "vitest run",
    "benchmark": "node benchmark/run.mjs",
    "lint": "prettier --check ."
  },
  "dependencies": {
    "fastify": "^5.0.0",
    "better-sqlite3": "^12.0.0",
    "sqlite-vec": "^0.1.6",
    "@modelcontextprotocol/sdk": "^1.0.0",
    "js-yaml": "^4.1.0"
  },
  "devDependencies": {
    "vitest": "^2.1.0",
    "prettier": "^3.3.0"
  },
  "engines": {
    "node": ">=20"
  }
}
